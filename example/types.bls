type MyVariant =
  | Yes
  | No of String

type MyTuple = int * int

class MyClass param0 =
  let times_two () = param0 * 2

abstract class MyMultiplier n =
  abstract get_factor -> int

  let apply () = n * (get_factor ())

class TimesTwo n extends (MyMultiplier n) =
  let get_factor = 2

type T MyGeneric = List<T> * T

type T MyRecord = { value: T; tag: String }
